<div class="max-w-4xl mx-auto p-8 flex flex-col gap-8">
	<div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative">
		<strong class="font-bold">Registration Successful!</strong>
		<span class="block sm:inline"> You've registered for <% if (moduleNumber===1) { %> Module 1: The Leadership Voice Masterclass <% } else if (moduleNumber===2) { %> Module 2: Advanced Leadership Communication <% } else if (moduleNumber===3) { %> Module 3: Executive Presence & Influence <% } %> </span>
	</div>
	<div class="bg-white rounded-xl shadow-2xl p-8 max-w-4xl mx-auto">
		<h2 class="text-2xl font-bold text-gray-800 text-center mb-8">Complete your registration by choosing a payment method</h2>
		<!-- Removed separate data attributes -->
		<!-- <div class="grid md:grid-cols-2 gap-8"> -->
		<div class="grid">
			<!-- Stripe Payment Option -->
			<div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg shadow-lg p-8 transform hover:scale-105 transition-transform duration-300">
				<div class="flex items-center justify-center mb-6"></div>
				<a href="<%= paymentLinks.stripe %>" class="block w-full py-4 px-6 bg-gradient-to-r from-blue-600 to-blue-700 text-white text-lg font-semibold text-center rounded-lg hover:from-blue-700 hover:to-blue-800 transform hover:-translate-y-1 transition-all duration-300 shadow-md"> Pay with Card </a>
			</div>
			<!-- PayPal Payment Option -->
			<!-- <div
        class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg shadow-lg p-8 transform hover:scale-105 transition-transform duration-300">
        <div class="flex items-center justify-center">
          <div id="paypal-button-container" class="w-full"></div>
        </div>
      </div> -->
		</div>
		<!-- Initialize the PayPal JS-SDK -->
		<script>
			// Store only the user email in the window object
			// The rest of the data will be fetched from the server
			window.moduleData = {
				userEmail: "<%= user.email %>",
			};
			console.log("User email for PayPal:", "<%= user.email %>");
		</script>
		<!-- <script src="https://www.paypal.com/sdk/js?client-id=<%= process.env.PAYPAL_CLIENT_ID %>&buyer-country=US&currency=USD&components=buttons&disable-funding=venmo,paylater,card" data-sdk-integration-source="developer-studio"></script>
		<script src="/javascript/paypal.js"></script> -->
	</div>
	<%- include(`${moduleTemplate}`) %>
</div>
